import{createReadStream,existsSync,statSync}from"node:fs";import{createServer}from"node:http";import{extname,join,normalize}from"node:path";
const root=process.cwd(),port=Number(process.env.PORT||4173),types={".html":"text/html; charset=utf-8",".css":"text/css; charset=utf-8",".js":"text/javascript; charset=utf-8"};
createServer((req,res)=>{const name=decodeURIComponent(new URL(req.url,`http://${req.headers.host}`).pathname),path=normalize(join(root,name==="/"?"/index.html":name));if(!path.startsWith(root)||!existsSync(path)||!statSync(path).isFile()){res.writeHead(404);res.end("Not found");return}res.writeHead(200,{"Content-Type":types[extname(path)]||"application/octet-stream","Cache-Control":"no-store"});createReadStream(path).pipe(res)}).listen(port,()=>console.log(`Daymark: http://localhost:${port}`));
